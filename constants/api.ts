const API_ROOT = "/api";

export default {
  grades: {
    add: `${API_ROOT}/grades/add`,
    fetch: `${API_ROOT}/grades/fetch`,
  },
  numbers: {
    add: `${API_ROOT}/numbers/add`,
    fetch: `${API_ROOT}/numbers/fetch`,
  },
};
